import React from 'react';

/** Sacchetto asporto — icona dalla reference PNG. */
export default function TakeoutBagIcon({ className = '' }) {
  return (
    <img
      src="/brand/takeout-bag-icon.png"
      alt=""
      className={className}
      draggable={false}
    />
  );
}
